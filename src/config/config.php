<?php

return array(
    
    /*
    |--------------------------------------------------------------------------
    | Path where XML files are stored
    |--------------------------------------------------------------------------
    |
    | Location where XML files may be stored. The filename is defined on each
    | XML model on property $file, similar to property $table on Eloquent model.
    |
    */
    
    'path' => storage_path() . '/xml',
    
);